# mutual-distillation

该项目示例化了互蒸馏训练流程，现在默认直接从开源数据源获取沪深300指数成分股的日线行情，构建时间序列监督学习数据集。

## 数据来源

* 通过 [akshare](https://akshare.xyz/) 拉取沪深300成分股列表及其前复权日线数据。
* 首次运行会将下载的数据缓存到 `./data` 目录下的 Pickle 文件，后续重复运行会直接读取缓存以避免重复请求。

请确保运行环境具备外网访问能力，并已安装 `requirements.txt` 中列出的依赖（包括 `akshare` 与 `pandas`）。